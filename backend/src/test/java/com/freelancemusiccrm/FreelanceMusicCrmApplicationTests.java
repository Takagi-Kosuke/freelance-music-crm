package com.freelancemusiccrm;

import org.junit.jupiter.api.Test;

/**
 * Smoke test — verifies the application context loads without errors.
 * Full integration tests use Testcontainers (PostgreSQL) and are in separate test classes.
 */
class FreelanceMusicCrmApplicationTests {

    @Test
    void contextLoads() {
        // Application context smoke-test runs in CI via Testcontainers.
        // This placeholder keeps the test suite non-empty for the initial build.
    }
}
