version: '3.9'

services:
  postgres:
    image: postgres:15-alpine
    container_name: freelance_music_crm_db
    environment:
      POSTGRES_DB: freelance_music_crm
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d freelance_music_crm"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
